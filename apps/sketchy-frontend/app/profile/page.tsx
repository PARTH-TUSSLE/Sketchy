import { Profile } from "../components/Profile";

export const metadata = {
  title: "Profile — Sketchy",
  description:
    "Your Sketchy profile — your name, your avatar, and every room you've opened.",
};

export default function ProfilePage() {
  return <Profile />;
}