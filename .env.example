# Copy to .env and fill in real values. NEVER commit this file's values.
# At least 32 characters, cryptographically random.
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Backends
PORT_HTTP=8380
PORT_WS=8381